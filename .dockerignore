# VCS & editor cruft
.git
.gitignore
.gitattributes
**/.DS_Store
**/*.swp
.vscode
.idea

# Dependencies (rebuilt in the image)
node_modules
**/node_modules

# Next.js build outputs (we build inside the image)
.next
out

# Logs & coverage
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
logs
*.log
coverage

# OS / misc
Thumbs.db

# Env files (don’t bake secrets into the image build context)
.env
.env.*
!.env.example

# Docker
Dockerfile~*
docker-compose*.yml~*